fn main() {
    let x = 5;
    let y = 10;
    println!("should be false: {}", x == y);
    println!("should be true: {}", x != y);
    println!("should be false {}", x > y);
    println!("should be true {}", x < y);
    println!("should be false {}", x >= y);
    println!("should be true {}", x <= y);
}
